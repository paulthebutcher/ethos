{
  "name": "aiethos",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "new-app": "./scripts/new-app.sh"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
